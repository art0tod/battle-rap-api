meta {
  name: Approve profile change request
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/mod/profile-changes/:id/approve
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
