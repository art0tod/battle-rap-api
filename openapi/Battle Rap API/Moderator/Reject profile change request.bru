meta {
  name: Reject profile change request
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/mod/profile-changes/:id/reject
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": ""
  }
}
