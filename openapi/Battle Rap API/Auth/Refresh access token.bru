meta {
  name: Refresh access token
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/refresh
  body: none
  auth: apikey
}

headers {
  refresh_token: {{apiKey}}
}

auth:apikey {
  key: refresh_token
  value: {{apiKey}}
  placement: header
}
