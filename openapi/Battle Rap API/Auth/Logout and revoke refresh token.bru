meta {
  name: Logout and revoke refresh token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: apikey
}

headers {
  refresh_token: {{apiKey}}
}

auth:apikey {
  key: refresh_token
  value: {{apiKey}}
  placement: header
}
