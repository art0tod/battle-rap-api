meta {
  name: Finalize battle and refresh views
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/admin/finalize/battles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
